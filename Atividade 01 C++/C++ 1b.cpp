#include <iostream> // Inclui a biblioteca padrão para entrada e saída


int main() { // Adiciona a função principal 'main', que é o ponto de entrada do program

// Declara uma variável do tipo inteiro
    double   numero; 

 // Solicita ao usuário para digitar um número
    std::cout << "Digite um número inteiro: ";
    
 // Lê o número digitado pelo usuário e armazena na variável
    std::cin >> numero; 

    // Exibe o valor da variável

// Retorna 0 para indicar que o programa terminou com sucesso
    return 0; 
    
}
